# elimination-template-nobuild 通用模板文档

## 1. 模板定位

`elimination-template-nobuild` 是一个基于 `CDN + 原生 ESM + 纯 JavaScript + 无构建` 的游戏模板。

它适合做的事情：

- 快速搭建一个可直接运行的小游戏模板
- 基于已有玩法改造成新的同类游戏
- 先完成玩法验证、视觉验证、参数验证，再决定是否工程化
- 以 `kernel / renderer / config` 分层方式组织逻辑

它当前内置的是一套 `Block Placement + Line Clear + Gem Target` 示例，但模板本身不是只服务这一种游戏。  
如果要做新的模版，推荐保留当前分层方式，而不是直接复制单一游戏逻辑。

---

## 2. 运行方式

模板当前运行方式：

- 浏览器直接运行 ESM 模块
- 第三方依赖通过 `index.html` 的 import map 从 CDN 引入
- 不依赖打包器
- 需要使用静态服务器启动

推荐启动方式：

```bash
python -m http.server 8080
```

或：

```bash
npx serve .
```

不要直接使用 `file://` 打开，否则模块、媒体资源、音视频行为都可能异常。

---

## 3. 整体架构

模板当前遵循四层结构：

1. 启动层  
   负责挂载页面、创建 kernel、挂载 renderer。

2. 配置层  
   负责集中管理参数，并向运行时提供统一读取能力。

3. 规则层  
   负责玩法逻辑、状态推进、得分、胜负、目标与 snapshot 输出。

4. 表现层  
   负责 Pixi 渲染、媒体背景、动画、拖拽、输入映射、HUD 与反馈。

一句话数据流：

`config.json -> config.js -> kernel -> snapshot -> renderer -> 用户输入 -> kernel.dispatch()`

核心原则：

- `kernel` 只负责规则
- `renderer` 只负责表现
- `config` 只负责参数
- `main` 只负责启动装配

---

## 4. 当前主运行链

当前真实运行路径如下：

- 页面入口：`index.html`
- 启动入口：`src/main.js`
- kernel 选择：`src/core/kernelFactory.js`
- 当前主 kernel：`src/kernels/blockBlastKernel.js`
- 当前主 renderer：`src/renderers/blockPlacement/mountBlockPlacementRenderer.js`
- 主配置文件：`src/config.json`
- 配置读取层：`src/config.js`

当前有两个历史/参考文件需要注意：

- `src/kernels/blockPlacementKernel.ts`
  - TypeScript 镜像 / 参考文件，不是当前 no-build 主运行文件
- `src/renderers/pixiTemplateRenderer.js`
  - 早期保留 renderer，不是当前主渲染路径

如果别人要基于这个模板做新模版，应该优先理解和修改的是上面那 7 个文件。

---

## 5. 目录职责

### 根目录

- `index.html`
  - 页面入口
  - import map 配置
  - 外层容器和基础 meta 配置

- `README.md`
  - 项目概览与快速运行说明

- `ARCHITECTURE.md`
  - 架构边界简版说明

- `SKILL.md`
  - 给模型使用的执行规约
  - 重点说明“改哪个文件会影响什么”

- `策划案.md`
  - 当前内置游戏的玩法、目标、视觉与配置基线

- `通用模板文档.md`
  - 当前文件
  - 给基于此模板继续做新模板的人看的总览文档

### `src/`

- `src/main.js`
  - 启动程序
  - 创建 kernel
  - 挂载 renderer

- `src/config.json`
  - 单一配置源
  - 保存玩法参数、表现参数、主题、文案、媒体地址

- `src/config.js`
  - 配置读取层
  - 负责统一读取、兼容映射、聚合列表型配置

### `src/core/`

- `src/core/kernelFactory.js`
  - 根据 `gameplay_mechanics_active` 决定使用哪个 kernel

- `src/core/contracts.js`
  - 放运行时共享的最小结构
  - 当前主要提供 overlay 数据结构 helper

### `src/kernels/`

- `src/kernels/blockBlastKernel.js`
  - 当前运行中的主规则文件
  - 负责棋盘、出块、放置、清线、分数、gem、胜负、snapshot

- `src/kernels/blockPlacementKernel.ts`
  - 当前 JS kernel 的 TS 镜像 / 参考版本

- `src/kernels/shapeLibrary.js`
  - shape 库
  - 别名归一化

- `src/kernels/placeholderKernel.js`
  - 未实现玩法模式时的占位 kernel

### `src/systems/`

- `src/systems/lineClearSystem.js`
  - 可复用纯规则逻辑
  - 当前主要负责清线计算

### `src/renderers/`

- `src/renderers/blockPlacement/mountBlockPlacementRenderer.js`
  - 当前主 renderer
  - 负责：
    - Pixi Application 初始化
    - 背景图片 / gif / 视频 / 音频
    - HUD 绘制
    - 棋盘绘制
    - 拖拽和投影
    - 粒子和动画
    - overlay 和按钮交互

- `src/renderers/common/ParticleSystem.js`
  - 通用粒子系统

- `src/renderers/pixiTemplateRenderer.js`
  - 历史保留 renderer

### `src/styles/`

- `src/styles/ui-layer.css`
  - 历史保留样式文件
  - 当前主视觉不依赖它完成核心 UI

---

## 6. `config.json` 的职责

`src/config.json` 是模板最核心的参数文件。

它的职责不是保存运行时状态，而是保存“可配置输入”。

它当前负责：

- 玩法参数
- UI 文案
- 颜色与主题
- 背景资源
- HUD 面板样式
- 棋盘样式
- 方块材质样式
- 分数与目标参数

### 6.1 当前结构

当前每个配置项统一为：

```json
"theme_primary": {
  "value": "oklch(0.756 0.147 235.2)",
  "type": "color",
  "label": "主色"
}
```

字段含义：

- `value`
  - 实际配置值
- `type`
  - 类型提示，用于配置面板或模型理解
- `label`
  - 中文说明

颜色值补充说明：

- 当前模板颜色字段不再局限于十六进制
- 只要是浏览器可识别的 CSS 颜色字符串，当前 renderer 都可以解析
- 默认优先使用：
  - `oklch(...)`
- 只有在以下情况才建议退回其他格式：
  - 现有设计稿或品牌色直接提供的是 hex / rgb / hsl
  - 某个目标颜色暂时不方便用 `oklch(...)` 微调
  - 需要兼容历史配置并减少迁移成本
- 可接受的备用格式：
  - `#RRGGBB`
  - `rgb(...)`
  - `hsl(...)`

### 6.2 当前命名规则

采用平铺 key，格式为：

`domain_subdomain_field`

例如：

- `gameplay_board_rows`
- `presentation_hud_rule_text`
- `theme_background_media`

### 6.3 只读规则

以 `_` 开头的 key 视为只读或元信息，例如：

- `_comment`
- `_meta_description`

如果未来要做可视化配置面板，通常可以直接把 `_` 开头项视为不可编辑。

### 6.4 当前配置域

#### `gameplay_*`

负责：

- 棋盘大小
- 回合方块数
- 初始棋盘状态
- shape 池
- gem 目标
- 计分参数
- 当前玩法模式标识

#### `presentation_*`

负责：

- UI 模板标识
- 动画策略
- 拖拽预览 / 合法高亮开关
- HUD 标题与规则文案

#### `theme_*`

负责：

- 主色 / 强调色 / 发光风格
- 背景渐变、背景媒体、背景音乐
- 棋盘面板样式
- HUD 面板样式
- 蒙层
- 方块材质和默认颜色池

---

## 7. `config.json` 中的列表型配置写法

这个模板当前不再用旧式数组结构去写这些字段：

- 初始棋盘格子列表
- shape 列表
- gem 目标列表
- 默认颜色池

而是改成“平铺 key + 序号”的方式。

### 7.1 初始棋盘格子

例如第 1 个预填充格子：

- `gameplay_initial_board_cell_1_row`
- `gameplay_initial_board_cell_1_col`
- `gameplay_initial_board_cell_1_color`
- `gameplay_initial_board_cell_1_gem_type`

如果要新增第 4 个格子，就继续补：

- `gameplay_initial_board_cell_4_row`
- `gameplay_initial_board_cell_4_col`
- `gameplay_initial_board_cell_4_color`
- `gameplay_initial_board_cell_4_gem_type`

### 7.2 shape 池

例如：

- `gameplay_shape_1`
- `gameplay_shape_2`
- `gameplay_shape_3`

### 7.3 gem 目标

例如：

- `gameplay_gem_target_1_type`
- `gameplay_gem_target_1_count`
- `gameplay_gem_target_2_type`
- `gameplay_gem_target_2_count`

### 7.4 默认颜色池

例如：

- `theme_piece_color_1`
- `theme_piece_color_2`
- `theme_piece_color_3`
- `theme_piece_color_4`

这套写法的优点是：

- 结构简单
- 易于平铺展示
- 易于做配置面板
- 易于兼容历史读取逻辑

---

## 8. `config.js` 的职责

`src/config.js` 是模板的配置读取层。

它的目标不是“把配置复制一遍”，而是解决以下问题：

1. 统一读取配置
2. 兼容旧写法
3. 把平铺 key 聚合成运行时需要的数组 / 对象

### 8.1 当前主要能力

#### `getSetting(path, fallback)`

最常用的配置读取入口。

作用：

- 读取平铺 key
- 兼容旧点路径
- 兼容部分分组对象
- 在拿不到值时返回默认值

例如：

```js
getSetting("gameplay_board_rows", 8)
getSetting("theme_background_media", "")
getSetting("presentation.interaction", {})
```

#### `getConfigEntry(path)`

返回原始配置叶子对象，用于拿到：

- `value`
- `type`
- `label`

#### `isConfigPathEditable(path)`

判断某个配置路径是否可编辑。

#### `listConfigLeafPaths()`

列出当前全部叶子路径。

#### `listEditableConfigPaths()`

列出当前全部可编辑路径。

#### `getBackgroundMediaSettings()`

统一获取背景媒体地址和类型。

### 8.2 当前聚合 helper

#### `buildThemePieceColors()`

把：

- `theme_piece_color_1`
- `theme_piece_color_2`
- `theme_piece_color_3`

聚合成运行时颜色数组。

#### `buildInitialBoardFilledCells()`

把：

- `gameplay_initial_board_cell_<n>_row`
- `gameplay_initial_board_cell_<n>_col`
- `gameplay_initial_board_cell_<n>_color`
- `gameplay_initial_board_cell_<n>_gem_type`

聚合成初始棋盘格子列表。

#### `buildShapePool()`

把：

- `gameplay_shape_<n>`

聚合成 shape 池数组。

#### `buildGemTargets()`

把：

- `gameplay_gem_target_<n>_type`
- `gameplay_gem_target_<n>_count`

聚合成 gem 目标数组。

### 8.3 为什么需要 `config.js`

如果只保留 `config.json`，运行层会出现两个问题：

- 每个文件都要自己解析平铺 key
- 旧字段兼容会散落到各个业务文件里

把这些逻辑集中到 `config.js` 的好处是：

- 规则文件更干净
- renderer 更专注表现
- 配置模型演进时只需要少量调整

---

## 9. `main.js` 的职责

`src/main.js` 非常轻，它只做启动装配。

当前代码逻辑非常明确：

```js
import { createKernel } from "./core/kernelFactory.js";
import { mountBlockPlacementRenderer } from "./renderers/blockPlacement/mountBlockPlacementRenderer.js";

async function bootstrap() {
  const root = document.querySelector("#app");
  if (!root) throw new Error("Missing #app container.");
  const kernel = createKernel();
  await mountBlockPlacementRenderer(root, kernel);
}

void bootstrap();
```

它的职责只有三件事：

1. 找到挂载容器
2. 创建 kernel
3. 挂载 renderer

这意味着：

- `main.js` 不应该承担玩法逻辑
- `main.js` 不应该承担具体 UI 细节
- `main.js` 不应该堆大量初始化分支

如果未来基于这个模板做新模版，`main.js` 应尽量继续保持轻量。

---

## 10. 如何基于当前模板做新的游戏模版

推荐顺序如下：

### 第一步：先判断是“复用模版”还是“新增玩法模版”

#### 复用现有玩法骨架

适合：

- 仍然是网格类 / 放置类 / 清线类玩法
- 只是改目标、节奏、视觉、规则细节

这时通常只需要改：

- `src/config.json`
- `src/config.js`
- `src/kernels/blockBlastKernel.js`
- `src/renderers/blockPlacement/mountBlockPlacementRenderer.js`

#### 新增新的玩法模式

适合：

- 新游戏和当前 block placement 差异较大
- 当前 kernel 结构不再适合直接扩展

这时建议：

1. 新建一个新的 kernel 文件
2. 在 `kernelFactory.js` 注册
3. 如有必要，新增一个新的 renderer
4. 保持配置层仍走 `config.json + config.js`

### 第二步：优先保留当前分层，不要先拆掉

即使是新游戏，也建议先保留以下思路：

- `config` 统一管参数
- `kernel` 统一管规则
- `renderer` 统一管表现
- `main` 统一管启动

### 第三步：优先让“参数变化”进入 `config.json`

例如下面这些变化，优先走配置而不是硬编码：

- 棋盘大小
- 分数参数
- 文案
- 背景媒体
- HUD 样式
- 方块默认颜色
- 胜利目标

### 第四步：只有“配置表达不了”的内容才去改代码

例如：

- 新的胜利规则
- 新的棋盘逻辑
- 新的掉落 / 合成 / 清除机制
- 新的拖拽反馈
- 新的动画系统

---

## 11. 哪些文件应该怎么理解

### `config.json`

把它当作：

- 模板参数数据库

不要把它当作：

- 运行时状态仓库
- 逻辑文件

### `config.js`

把它当作：

- 配置适配层

不要把它当作：

- 规则层
- renderer 层

### `main.js`

把它当作：

- 启动入口

不要把它当作：

- 核心业务承载层

### `blockBlastKernel.js`

把它当作：

- 当前示例游戏的规则实现

### `mountBlockPlacementRenderer.js`

把它当作：

- 当前示例游戏的视觉与交互实现

---

## 12. 通用改造建议

### 12.1 只换主题

优先改：

- `theme_*`
- `presentation_*`

### 12.2 只换玩法节奏

优先改：

- `gameplay_*`
- 必要时改 `blockBlastKernel.js`

### 12.3 只换 shape

优先改：

- `gameplay_shape_<n>`
- `src/kernels/shapeLibrary.js`

### 12.4 只换视觉表现

优先改：

- `theme_*`
- `mountBlockPlacementRenderer.js`

### 12.5 只换背景和媒体

优先改：

- `theme_background_media`
- `theme_background_media_type`
- `theme_background_music`
- `theme_scene_overlay_*`
- `theme_tray_overlay_*`

---

## 13. 基于模板继续扩展时的建议边界

建议继续保留：

- 单向数据流
- `snapshot` 驱动渲染
- 配置优先
- 平铺 key
- kernel / renderer 分离

不建议轻易破坏：

- 由 renderer 直接维护核心业务真状态
- 在多个文件里重复解析配置
- 让 `main.js` 变成大而杂的入口
- 让 `config.json` 回退成深层嵌套且不可控的结构

---

## 14. 总结

如果别人要基于你的 `elimination-template-nobuild` 去制作新的游戏模版，可以把它理解为：

一个已经具备以下特点的 no-build 游戏模板基座：

- 有明确入口
- 有稳定配置层
- 有可替换规则层
- 有可替换表现层
- 有清晰目录职责
- 有适合继续扩展成新模板的参数模型

最值得复用的不是当前这一个 block 游戏本身，而是这套模板方法：

- `config.json` 负责表达
- `config.js` 负责读取和聚合
- `main.js` 负责启动
- `kernel` 负责规则
- `renderer` 负责表现

只要继续沿用这套结构，就可以把当前模板稳定地演进成更多新的游戏模版。
